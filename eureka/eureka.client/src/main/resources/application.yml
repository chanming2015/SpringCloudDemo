server:
  port: 8762

eureka:
  client:
    serviceUrl:
      defaultZone: http://dev.eureka.server:8761/eureka/
spring:
  application:
    name: service-hi
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://oauth-server:8769/.auth/jwks.json